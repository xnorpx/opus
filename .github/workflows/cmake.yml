# cmake.yml is autogenerated by scripts/ci_generator.py do not edit by hand.
on:
- push
- pull_request
- pull_request_target
jobs:
  win-x86_64-custom-modes-off-fixed-point-off-float-api-on-intrinsics-off:
    name: win-x86_64-custom-modes-off-fixed-point-off-float-api-on-intrinsics-off
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v2
      with:
        fetch-depth: 0
    - run: mkdir build
      name: Create Work Dir
    - working-directory: build
      run: cmake .. -DOPUS_BUILD_PROGRAMS=ON -DOPUS_BUILD_TESTING=ON -DOPUS_CUSTOM_MODES=OFF
        -DOPUS_FIXED_POINT=OFF -DOPUS_ENABLE_FLOAT_API=ON -DOPUS_DISABLE_INTRINSICS=OFF
        -DCMAKE_BUILD_TYPE=Release -G Ninja -A x64
      name: Configure
    - working-directory: build
      run: cmake --build . --config Release
      name: Build
    - working-directory: build
      run: ctest -C Release
      name: Test
  win-x86_64-custom-modes-off-fixed-point-off-float-api-on-intrinsics-on:
    name: win-x86_64-custom-modes-off-fixed-point-off-float-api-on-intrinsics-on
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v2
      with:
        fetch-depth: 0
    - run: mkdir build
      name: Create Work Dir
    - working-directory: build
      run: cmake .. -DOPUS_BUILD_PROGRAMS=ON -DOPUS_BUILD_TESTING=ON -DOPUS_CUSTOM_MODES=OFF
        -DOPUS_FIXED_POINT=OFF -DOPUS_ENABLE_FLOAT_API=ON -DOPUS_DISABLE_INTRINSICS=ON
        -DCMAKE_BUILD_TYPE=Release -G Ninja -A x64
      name: Configure
    - working-directory: build
      run: cmake --build . --config Release
      name: Build
    - working-directory: build
      run: ctest -C Release
      name: Test
  win-x86_64-custom-modes-off-fixed-point-on-float-api-off-intrinsics-off:
    name: win-x86_64-custom-modes-off-fixed-point-on-float-api-off-intrinsics-off
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v2
      with:
        fetch-depth: 0
    - run: mkdir build
      name: Create Work Dir
    - working-directory: build
      run: cmake .. -DOPUS_BUILD_PROGRAMS=ON -DOPUS_BUILD_TESTING=ON -DOPUS_CUSTOM_MODES=OFF
        -DOPUS_FIXED_POINT=ON -DOPUS_ENABLE_FLOAT_API=OFF -DOPUS_DISABLE_INTRINSICS=OFF
        -DCMAKE_BUILD_TYPE=Release -G Ninja -A x64
      name: Configure
    - working-directory: build
      run: cmake --build . --config Release
      name: Build
    - working-directory: build
      run: ctest -C Release
      name: Test
  win-x86_64-custom-modes-off-fixed-point-on-float-api-off-intrinsics-on:
    name: win-x86_64-custom-modes-off-fixed-point-on-float-api-off-intrinsics-on
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v2
      with:
        fetch-depth: 0
    - run: mkdir build
      name: Create Work Dir
    - working-directory: build
      run: cmake .. -DOPUS_BUILD_PROGRAMS=ON -DOPUS_BUILD_TESTING=ON -DOPUS_CUSTOM_MODES=OFF
        -DOPUS_FIXED_POINT=ON -DOPUS_ENABLE_FLOAT_API=OFF -DOPUS_DISABLE_INTRINSICS=ON
        -DCMAKE_BUILD_TYPE=Release -G Ninja -A x64
      name: Configure
    - working-directory: build
      run: cmake --build . --config Release
      name: Build
    - working-directory: build
      run: ctest -C Release
      name: Test
  win-x86_64-custom-modes-off-fixed-point-on-float-api-on-intrinsics-off:
    name: win-x86_64-custom-modes-off-fixed-point-on-float-api-on-intrinsics-off
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v2
      with:
        fetch-depth: 0
    - run: mkdir build
      name: Create Work Dir
    - working-directory: build
      run: cmake .. -DOPUS_BUILD_PROGRAMS=ON -DOPUS_BUILD_TESTING=ON -DOPUS_CUSTOM_MODES=OFF
        -DOPUS_FIXED_POINT=ON -DOPUS_ENABLE_FLOAT_API=ON -DOPUS_DISABLE_INTRINSICS=OFF
        -DCMAKE_BUILD_TYPE=Release -G Ninja -A x64
      name: Configure
    - working-directory: build
      run: cmake --build . --config Release
      name: Build
    - working-directory: build
      run: ctest -C Release
      name: Test
  win-x86_64-custom-modes-off-fixed-point-on-float-api-on-intrinsics-on:
    name: win-x86_64-custom-modes-off-fixed-point-on-float-api-on-intrinsics-on
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v2
      with:
        fetch-depth: 0
    - run: mkdir build
      name: Create Work Dir
    - working-directory: build
      run: cmake .. -DOPUS_BUILD_PROGRAMS=ON -DOPUS_BUILD_TESTING=ON -DOPUS_CUSTOM_MODES=OFF
        -DOPUS_FIXED_POINT=ON -DOPUS_ENABLE_FLOAT_API=ON -DOPUS_DISABLE_INTRINSICS=ON
        -DCMAKE_BUILD_TYPE=Release -G Ninja -A x64
      name: Configure
    - working-directory: build
      run: cmake --build . --config Release
      name: Build
    - working-directory: build
      run: ctest -C Release
      name: Test
  win-x86_64-custom-modes-on-fixed-point-off-float-api-on-intrinsics-off:
    name: win-x86_64-custom-modes-on-fixed-point-off-float-api-on-intrinsics-off
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v2
      with:
        fetch-depth: 0
    - run: mkdir build
      name: Create Work Dir
    - working-directory: build
      run: cmake .. -DOPUS_BUILD_PROGRAMS=ON -DOPUS_BUILD_TESTING=ON -DOPUS_CUSTOM_MODES=ON
        -DOPUS_FIXED_POINT=OFF -DOPUS_ENABLE_FLOAT_API=ON -DOPUS_DISABLE_INTRINSICS=OFF
        -DCMAKE_BUILD_TYPE=Release -G Ninja -A x64
      name: Configure
    - working-directory: build
      run: cmake --build . --config Release
      name: Build
    - working-directory: build
      run: ctest -C Release
      name: Test
  win-x86_64-custom-modes-on-fixed-point-off-float-api-on-intrinsics-on:
    name: win-x86_64-custom-modes-on-fixed-point-off-float-api-on-intrinsics-on
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v2
      with:
        fetch-depth: 0
    - run: mkdir build
      name: Create Work Dir
    - working-directory: build
      run: cmake .. -DOPUS_BUILD_PROGRAMS=ON -DOPUS_BUILD_TESTING=ON -DOPUS_CUSTOM_MODES=ON
        -DOPUS_FIXED_POINT=OFF -DOPUS_ENABLE_FLOAT_API=ON -DOPUS_DISABLE_INTRINSICS=ON
        -DCMAKE_BUILD_TYPE=Release -G Ninja -A x64
      name: Configure
    - working-directory: build
      run: cmake --build . --config Release
      name: Build
    - working-directory: build
      run: ctest -C Release
      name: Test
  win-x86_64-custom-modes-on-fixed-point-on-float-api-off-intrinsics-off:
    name: win-x86_64-custom-modes-on-fixed-point-on-float-api-off-intrinsics-off
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v2
      with:
        fetch-depth: 0
    - run: mkdir build
      name: Create Work Dir
    - working-directory: build
      run: cmake .. -DOPUS_BUILD_PROGRAMS=ON -DOPUS_BUILD_TESTING=ON -DOPUS_CUSTOM_MODES=ON
        -DOPUS_FIXED_POINT=ON -DOPUS_ENABLE_FLOAT_API=OFF -DOPUS_DISABLE_INTRINSICS=OFF
        -DCMAKE_BUILD_TYPE=Release -G Ninja -A x64
      name: Configure
    - working-directory: build
      run: cmake --build . --config Release
      name: Build
    - working-directory: build
      run: ctest -C Release
      name: Test
  win-x86_64-custom-modes-on-fixed-point-on-float-api-off-intrinsics-on:
    name: win-x86_64-custom-modes-on-fixed-point-on-float-api-off-intrinsics-on
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v2
      with:
        fetch-depth: 0
    - run: mkdir build
      name: Create Work Dir
    - working-directory: build
      run: cmake .. -DOPUS_BUILD_PROGRAMS=ON -DOPUS_BUILD_TESTING=ON -DOPUS_CUSTOM_MODES=ON
        -DOPUS_FIXED_POINT=ON -DOPUS_ENABLE_FLOAT_API=OFF -DOPUS_DISABLE_INTRINSICS=ON
        -DCMAKE_BUILD_TYPE=Release -G Ninja -A x64
      name: Configure
    - working-directory: build
      run: cmake --build . --config Release
      name: Build
    - working-directory: build
      run: ctest -C Release
      name: Test
  win-x86_64-custom-modes-on-fixed-point-on-float-api-on-intrinsics-off:
    name: win-x86_64-custom-modes-on-fixed-point-on-float-api-on-intrinsics-off
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v2
      with:
        fetch-depth: 0
    - run: mkdir build
      name: Create Work Dir
    - working-directory: build
      run: cmake .. -DOPUS_BUILD_PROGRAMS=ON -DOPUS_BUILD_TESTING=ON -DOPUS_CUSTOM_MODES=ON
        -DOPUS_FIXED_POINT=ON -DOPUS_ENABLE_FLOAT_API=ON -DOPUS_DISABLE_INTRINSICS=OFF
        -DCMAKE_BUILD_TYPE=Release -G Ninja -A x64
      name: Configure
    - working-directory: build
      run: cmake --build . --config Release
      name: Build
    - working-directory: build
      run: ctest -C Release
      name: Test
  win-x86_64-custom-modes-on-fixed-point-on-float-api-on-intrinsics-on:
    name: win-x86_64-custom-modes-on-fixed-point-on-float-api-on-intrinsics-on
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v2
      with:
        fetch-depth: 0
    - run: mkdir build
      name: Create Work Dir
    - working-directory: build
      run: cmake .. -DOPUS_BUILD_PROGRAMS=ON -DOPUS_BUILD_TESTING=ON -DOPUS_CUSTOM_MODES=ON
        -DOPUS_FIXED_POINT=ON -DOPUS_ENABLE_FLOAT_API=ON -DOPUS_DISABLE_INTRINSICS=ON
        -DCMAKE_BUILD_TYPE=Release -G Ninja -A x64
      name: Configure
    - working-directory: build
      run: cmake --build . --config Release
      name: Build
    - working-directory: build
      run: ctest -C Release
      name: Test
name: CMake Build Matrix
